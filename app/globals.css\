/* === Testimonials animation (server-safe) === */
@keyframes fadeInOut {
  0%, 90%, 100% { opacity: 0; }
  5%, 45% { opacity: 1; }
}

.animate-fadeInOut {
  animation: fadeInOut 55s linear infinite;
}
